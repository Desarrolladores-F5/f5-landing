import Image from "next/image";
import Link from "next/link";
import { Button } from "@/components/ui/button";

export const metadata = {
  title: "Mejores prácticas para CI/CD en Next.js",
  description:
    "Estrategia de ramas, checks automáticos, despliegue y rollback con pipelines reproducibles en Next.js.",
};

export default function BlogPost() {
  return (
    <article className="mx-auto max-w-4xl px-4 sm:px-6 lg:px-8 py-10">
      {/* Imagen destacada */}
      <Image
        src="/images/blog/b2.jpg"
        alt="Pipeline de CI/CD para Next.js"
        width={1280}
        height={720}
        className="rounded-2xl mb-10 object-cover border"
        priority
      />

      {/* Título */}
      <header className="mb-8">
        <h1 className="text-5xl md:text-6xl font-extrabold tracking-tight leading-tight text-slate-900">
          Mejores prácticas para CI/CD en Next.js
        </h1>
        <p className="mt-5 text-lg text-slate-600 max-w-3xl text-justify">
          Una cadena de <strong>CI/CD</strong> (Integración Continua y
          Despliegue Continuo) bien implementada es el puente entre tu equipo y
          tus usuarios finales. No solo mejora la eficiencia técnica, también
          impacta directamente en el negocio: más velocidad de entrega,
          reducción de errores, mejor colaboración y mayor confianza en cada
          versión publicada.
        </p>
      </header>

      <div
        className={[
          "prose prose-slate md:prose-lg max-w-none",
          "prose-headings:scroll-mt-24",
          "prose-h2:text-3xl md:prose-h2:text-4xl prose-h2:font-bold prose-h2:tracking-tight",
          "prose-h3:text-xl md:prose-h3:text-2xl prose-h3:font-semibold prose-h3:tracking-tight",
          "prose-p:text-justify prose-li:text-justify prose-blockquote:text-justify",
        ].join(" ")}
      >
        {/* Beneficios */}
        <section className="pt-10 mt-10 border-t border-slate-200">
          <h2>Beneficios directos para tu empresa</h2>
          <p>
            Implementar CI/CD en tu proyecto con Next.js no es simplemente una
            mejora técnica. Sus beneficios se reflejan directamente en el
            crecimiento de tu negocio y en la experiencia de tus clientes:
          </p>
          <ul>
            <li>
              <strong>Menos incidentes en producción:</strong> los errores se
              detectan antes gracias a validaciones automáticas, evitando
              interrupciones que generan pérdidas económicas y de reputación.
            </li>
            <li>
              <strong>Feedback más rápido:</strong> cada Pull Request genera una
              vista previa para validar cambios visuales y funcionales antes de
              publicarlos.
            </li>
            <li>
              <strong>Riesgo controlado:</strong> con feature flags y rollback
              en segundos, evitando que los clientes sufran fallos prolongados.
            </li>
            <li>
              <strong>Escalabilidad del equipo:</strong> al unificar procesos,
              todos trabajan con los mismos estándares de calidad y despliegue,
              reduciendo fricción entre desarrolladores, QA y operaciones.
            </li>
          </ul>
        </section>

        {/* Flujo */}
        <section className="pt-10 mt-10 border-t border-slate-200">
          <h2>Flujo recomendado de CI/CD</h2>
          <p>
            Un pipeline bien diseñado sigue pasos claros que aseguran calidad y
            control en cada versión:
          </p>
          <ol>
            <li>
              <strong>Feature:</strong> crea una rama <code>feature/*</code> y
              abre un Pull Request hacia <code>develop</code>.
            </li>
            <li>
              <strong>Checks automáticos:</strong> validación de tipado,
              análisis de código, tests y build + preview automático.
            </li>
            <li>
              <strong>Integración:</strong> al aprobar el PR, el merge en{" "}
              <code>develop</code> dispara la integración continua.
            </li>
            <li>
              <strong>Release:</strong> el merge en <code>main</code> (rama
              protegida) lanza el despliegue en producción.
            </li>
            <li>
              <strong>Monitoreo:</strong> health checks constantes +
              auto-rollback en caso de fallos.
            </li>
          </ol>
          <blockquote>
            💡 <strong>Tip:</strong> utiliza <em>concurrency</em> para cancelar
            pipelines antiguos y habilita <code>branch protection</code> en{" "}
            <code>main</code> para evitar errores humanos.
          </blockquote>
        </section>

        {/* Estrategia de ramas */}
        <section className="pt-10 mt-10 border-t border-slate-200">
          <h2>Estrategia de ramas recomendada</h2>
          <p>
            Una buena estrategia de ramas es clave para mantener ordenado el
            ciclo de desarrollo y evitar conflictos:
          </p>
          <ul>
            <li>
              <code>main</code> → producción (protegida, requiere revisiones y
              checks).
            </li>
            <li>
              <code>develop</code> → integración y QA, el ambiente donde se
              validan cambios antes de liberar.
            </li>
            <li>
              <code>feature/*</code> → ramas de desarrollo diarias, PR hacia{" "}
              <code>develop</code>.
            </li>
            <li>
              <code>hotfix/*</code> (opcional) → arreglos urgentes que van
              directo a producción.
            </li>
          </ul>
        </section>

        {/* Checks */}
        <section className="pt-10 mt-10 border-t border-slate-200">
          <h2>Checks mínimos que deben pasar</h2>
          <p>
            Para garantizar calidad en cada despliegue, estos son los{" "}
            <strong>controles imprescindibles</strong> que tu pipeline debe
            ejecutar:
          </p>
          <ul>
            <li>
              <strong>Typecheck:</strong> <code>tsc --noEmit</code>
            </li>
            <li>
              <strong>Lint:</strong> <code>next lint</code>
            </li>
            <li>
              <strong>Tests:</strong> unitarias e integración con Vitest/Jest +
              React Testing Library
            </li>
            <li>
              <strong>Build:</strong> <code>next build</code> para asegurar que
              la app compila sin errores.
            </li>
          </ul>
        </section>

        {/* Pipeline */}
        <section className="pt-10 mt-10 border-t border-slate-200">
          <h2>Pipeline en GitHub Actions (ejemplo)</h2>
          <p>
            A continuación un pipeline de ejemplo para{" "}
            <strong>GitHub Actions</strong>. Valida tipado, lint, tests y build
            en cada push o PR:
          </p>
          <pre>
            <code>{`name: ci

on:
  pull_request:
    branches: [develop, main]
  push:
    branches: [develop, main]

concurrency:
  group: \${{ github.workflow }}-\${{ github.ref }}
  cancel-in-progress: true

jobs:
  ci:
    name: Lint • Test • Build
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: 18
          cache: npm

      - name: Install
        run: npm ci

      - name: Typecheck
        run: npm run typecheck || true

      - name: Lint
        run: npm run lint

      - name: Test
        run: npm test --if-present

      - name: Build
        run: npm run build`}</code>
          </pre>
        </section>

        {/* Cierre */}
        <section className="pt-10 mt-10 border-t border-slate-200">
          <h2>Conclusión</h2>
          <p className="text-justify">
            Adoptar CI/CD en Next.js es invertir en velocidad, calidad y
            seguridad. Con un pipeline sólido, puedes entregar nuevas
            funcionalidades más rápido, minimizar riesgos en producción y
            mantener la confianza de tus clientes. Establece una estrategia de
            ramas clara, asegura checks automáticos y habilita rollback
            inmediato: tu equipo dedicará menos tiempo a resolver errores y más
            a crear innovación.
          </p>
        </section>

        {/* Botón volver */}
        <div className="pt-12 mt-12 border-t border-slate-200 not-prose">
          <Button asChild size="lg" className="rounded-2xl">
            <Link href="/blog">← Volver al Blog</Link>
          </Button>
        </div>
      </div>
    </article>
  );
}
